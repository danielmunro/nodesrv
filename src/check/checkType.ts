export enum CheckType {
  HasTarget,
  HasItem,
  ItemPresent,
  ContainerPresent,
  IsPlayer,
  AuthorizationLevel,
  ValidSubject,
  Disposition,
  IsFighting,
  HasSkill,
  HasAffect,
  Unspecified,
}
