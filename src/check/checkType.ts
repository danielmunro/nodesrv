export enum CheckType {
  HasArguments,
  HasTarget,
  HasItem,
  ItemPresent,
  ContainerPresent,
  IsPlayer,
  Level,
  AuthorizationLevel,
  ValidSubject,
  Disposition,
  IsFighting,
  HasSkill,
  HasSpell,
  HasAffect,
  Unspecified,
}
